doctype html
html
  head
    base(href="/")
    title Анонимный клиент - IQChannels
    meta(charset="UTF-8")
    meta(name="viewport",content="width=device-width, initial-scale=1")

  body.example
    h1
      a(href="widget/example.html", style="text-decoration: none") Чаты
      span(style="opacity: 0.5") &nbsp;/&nbsp;Анонимный клиент

    p
      button(type="button", onclick="widget.open();") Открыть
      span &nbsp;
      button(type="button", onclick="widget.close();") Закрыть
      span &nbsp;
      button(type="button", onclick="widget.logout();") Выйти
      span &nbsp;
      button(type="button", onclick="widget.destroy();") Уничтожить
      button(type="button", onclick="configureWidget();") Настроить конфигурацию
        | &nbsp;
      br
      br
      span Текущий конфиг:
      pre(id="current_config")
